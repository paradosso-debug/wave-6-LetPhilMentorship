# Project 05 — Functions

## 🌱 Overview

Functions are reusable blocks of code. They help you avoid repetition, give names to actions, and make programs easier to read and test.

## 🧠 Learning Goals

- Write function **declarations** and call them.
- Pass **parameters** and read **arguments**.
- Return values and store them in variables.
- Use **arrow functions** for concise syntax.
- Compose functions (one function calling another).

## 🌍 Real-Life Relevance

| Concept        | Everyday Analogy              | Real-World Example                                |
| -------------- | ----------------------------- | ------------------------------------------------- |
| Function       | A kitchen recipe              | A `formatPrice()` function used all over your app |
| Parameters     | Recipe ingredients            | `sendEmail(to, subject, body)`                    |
| Return value   | Final dish                    | `getTotal(cart)` returns a number you display     |
| Arrow function | Quick sticky-note instruction | One-line helpers for sorting or mapping data      |
| Composition    | Assembly line                 | `sanitize()` → `validate()` → `save()` pipeline   |

## ✅ Checklist

- [ ] I can write and call a function by name.
- [ ] I can use parameters and pass arguments correctly.
- [ ] I can use and store a returned value.
- [ ] I can write concise arrow functions.
- [ ] I can compose functions to solve a task step by step.
