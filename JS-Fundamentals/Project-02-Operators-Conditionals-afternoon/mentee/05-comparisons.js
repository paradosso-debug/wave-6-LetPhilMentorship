// 🌱 Project 02 — Operators & Conditionals
// 🎯 Topic: Comparisons (> < >= <=)
// ------------------------------------------------------------
// Each file contains FIVE mini-examples (EX1–EX5).
// For mentees: follow the steps (comments only).
// For mentors: same steps + working code for each EX.
// ------------------------------------------------------------
// EX1: Check if age >= 18; log true/false.
// EX2: Check if temp < 0 for freezing.
// EX3: Check if stock >= minStock.
// EX4: Check if speed > limit.
// EX5: Check if score <= maxScore.

const age = 17;

const checkAge = age >= 18;

console.log(checkAge);

const temp = -1;

const checkTemp = temp < 0;
console.log(checkTemp);

const stock = 10;
const minStock = 12;

const check = stock >= minStock;

console.log(check);

let speed = 40;
let limit = 5;
console.log(speed > limit);

const score = 80;

const maxScore = 100;

console.log(score <= maxScore);
